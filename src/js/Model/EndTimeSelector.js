const openEndTimeList = () => {
    // Hide button

    // Show list
}

const closeEndTimeList = () => {
    // Hide list

    // Show button
}

const selectEndTime = () => {
    // Update end time state

    // Hide list

    // Show button with new end time from state
}

export { openEndTimeList, closeEndTimeList, selectEndTime }