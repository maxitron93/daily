import { state } from '../state'

// Increase Start Time
// New Start Decimal = (oldStartDecimal * oldNumHours + numHoursChange) / newNumHours

// Decrease Start Time
// New Start Deciaml = (oldStartDecimal * oldNumHours - numHoursChange) / newNumHours 

// Increase End Time
// New Start Deciaml = (oldStartDecimal * oldNumHours) / newNumHours

// Decrease End Time
// New Start Deciaml = (oldStartDecimal * oldNumHours) / newNumHours

// New Width = oldWidth * oldNumHours / newNumHours